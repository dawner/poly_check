{
  "name": "poly_check",
  "version": "0.1.0",
  "private": true,
  "description": "Simple application to check if an address is inside a polygon",
  "scripts": {
    "build": "webpack -p",
    "build:local": "webpack --config webpack.dev.js --progress --colors",
    "build:local:watch": "webpack --watch --config webpack.dev.js --progress --colors",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dawn Richardson",
  "license": "ISC",
  "devDependencies": {
    "dotenv-webpack": "^1.8.0",
    "prettier": "^2.0.5",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
