{
  "name": "poly_check",
  "version": "0.1.0",
  "description": "Simple application to check if an address is inside a polygon",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dawn Richardson",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
